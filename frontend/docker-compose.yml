services:
  frontend:
    build:
      context: .
      args:
        # Backend API URL - set to your backend domain (no trailing slash)
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000}
    ports:
      - "4173:80"

